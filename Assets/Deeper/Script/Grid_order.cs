using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Grid_order : MonoBehaviour
{
    // Start is called before the first frame update
    public int layer_order;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
